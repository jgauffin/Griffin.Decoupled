using System;
using System.Collections.Generic;
using System.Linq;
using System.Runtime.CompilerServices;
using System.Text;

namespace Griffin.Decoupled.Container
{
    /// <summary>
    /// Support package for Griffin.Container
    /// </summary>
    /// <remarks>Contains an adapter <see cref="ContainerAdapter"/> which you can use. You can also just call <code>yourContainer.DispatchCommands()</code> or <code>yourContainer.DispatchEvents()</code> to configure Griffin.Decoupled to use Griffin.Container for the dispatching (wont work with the pipeline dispatchers though)</remarks>
    [CompilerGenerated]
    class NamespaceDoc
    {
    }
}
